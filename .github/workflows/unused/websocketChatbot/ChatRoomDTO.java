package cdb.websocket_old.notusenow;
/*
 * package cdb.websocket.notusenow; import java.util.HashSet; import java.util.Set; import java.util.UUID; import
 * org.springframework.web.socket.WebSocketSession; import lombok.Getter; import lombok.Setter;
 * @Setter
 * @Getter public class ChatRoomDTO { public static ChatRoomDTO create(final String name) {// 채팅방 이름과 함께 만들기 final var
 * room = new ChatRoomDTO(); room.roomId = UUID.randomUUID().toString();// 방의 고유 식별번호 랜덤으로 생성 room.name = name;// 만들어진
 * 방의 이름 return room; } private String roomId; private String name; private Set<WebSocketSession> sessions = new
 * HashSet<>(); // 여러개의 채팅방의 세션을 저장하기 위함 }
 */